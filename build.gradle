/*
 * This file was generated by the Gradle 'init' task.
 *
 * This generated file contains a sample Java project to get you started.
 * For more details take a look at the Java Quickstart chapter in the Gradle
 * user guide available at https://docs.gradle.org/4.6/userguide/tutorial_java_projects.html
 */


plugins {
    // Apply the java plugin to add support for Java
    id 'java'

    // IDE support
    id 'idea'

    // Apply the application plugin to add support for building an application
    id 'application'
}

apply from: "$rootDir/repositories.gradle"

// Define the main class for the application
mainClassName = 'App'

dependencies {
    implementation 'com.gradle:build-scan-plugin:2.3'
    implementation 'org.springframework.boot:spring-boot-starter:2.1.6.RELEASE'

    implementation 'org.springframework.boot:spring-boot-dependencies:2.0.5.RELEASE'
    implementation 'org.springframework.boot:spring-boot-starter-web:2.1.6.RELEASE'

    testImplementation 'org.springframework.boot:spring-boot-starter-test:2.1.6.RELEASE'

    components {
        withModule('org.springframework:spring-beans') {
            allVariants {
                withDependencyConstraints {
                    // Need to patch constraints because snakeyaml is an optional dependency
                    it.findAll { it.name == 'snakeyaml' }.each { it.version { strictly '1.19' } }
                }
            }
        }
    }
}

// In this section you declare where to find the dependencies of your project
//repositories {
//    // Use jcenter for resolving your dependencies.
//    // You can declare any Maven/Ivy/file repository here.
//    mavenLocal()
//    maven { url "http://nexus-operations.cheshdatasys.co.uk:8081/repository/PublicRepositories/" }
//    maven { url "http://nexus-operations.cheshdatasys.co.uk:8081/repository/CDLRepositories/" }
//}
